import express from 'express';
const router = express.Router();

// Placeholder: Auth routes will go here

router.get('/', (req, res) => {
  res.send('Auth route placeholder');
});

export default router;
