import express from 'express';
const router = express.Router();

// Placeholder: Parking routes will go here

router.get('/', (req, res) => {
  res.send('Parking route placeholder');
});

export default router;
